##include $(top_srcdir)/Makefile.decl

NULL=

SUBDIRS=data src vapi

EXTRA_DIST = \
	libfmcore-$(LIBFMCORE_API_VERSION).pc.in \
	$(NULL)

pkgconfigdir = $(libdir)/pkgconfig
pkgconfig_DATA = \
	libfmcore-$(LIBFMCORE_API_VERSION).pc \
	$(NULL)

